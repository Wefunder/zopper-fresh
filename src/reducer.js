// The one reducer we will work with for this basic example.

import * as Actions from "./actions"

export default (state = {}, action) => {
  // switch (action.type) {
  //   case Actions.EXAMPLE_REDUX_ACTION_NAME:
  //     return { newState: true }
  // 
  //   default:
  //     return state
  // }
  return state
}
